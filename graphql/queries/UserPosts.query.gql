query UserPosts($params: FilterInput!) {
  userPosts(params: $params) {
    hasMore
    nextCursor
    data {
      iLiked
      likeCount
      replyCount
      post {
        _id
        ...PostFragment
      }
    }
  }
}
