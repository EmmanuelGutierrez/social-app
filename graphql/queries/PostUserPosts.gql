query PostUserPosts($params: FilterInput!) {
  PostUserPosts(params: $params) {
    hasMore
    nextCursor
    data {
      iLiked
      likeCount
      replyCount
      post {
        _id
        ...PostFragment
      }
    }
  }
}
