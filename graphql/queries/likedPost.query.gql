query LikedPosts($params: FilterInput!) {
  likedPosts(params: $params) {
    hasMore
    nextCursor
    data {
      iLiked
      likeCount
      replyCount
      post {
        _id
        ...PostFragment
      }
    }
  }
}
